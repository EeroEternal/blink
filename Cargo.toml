[package]
name = "blink-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.0", features = ["full"] }
libc = "0.2"
nix = { version = "0.27", features = ["socket", "poll", "sched", "process", "fs"] }
